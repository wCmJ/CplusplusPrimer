拷贝构造函数：
    第一个显示参数必须为引用类型：防止循环无限调用
    使用场景：
        1.  =初始化，type A = B;
        2.  作为实参传递给非引用类型
        3.  从非引用类型的函数返回一个对象
        4.  用花括号列表初始化一个数组中的元素或一个聚合类中的成员 
拷贝赋值函数：
    为了与内置类型的赋值保持一致，赋值运算符通常返回一个指向其左侧运算对象的引用。        
        
移动构造函数：

移动赋值函数：
析构函数：
    构造函数初始化对象的非static数据成员
    析构函数释放对象使用的资源，并销毁对象的非static数据成员
    无返回值，不接受参数。
    由于不接受参数，所以不能被重载。
    一个类，只会有唯一一个析构函数。
    内置类型没有析构函数，因此销毁内置类型成员什么也不需要做。
    隐式销毁一个内置指针类型不会delete它所指向的对象
    智能指针是类类型，具有析构函数。











